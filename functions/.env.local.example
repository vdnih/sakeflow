# エミュレータでリアルOpenAI呼び出しを使う場合、このファイルを .env.local としてコピーして設定する
# .env.local は *.local パターンにより git 管理対象外

# USE_REAL_AI=true にするとエミュレータでもOpenAI APIを呼び出す（未設定またはfalseはモックデータを返す）
USE_REAL_AI=true

OPENAI_API_KEY=sk-...
